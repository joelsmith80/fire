jQuery(document).ready(function($){

  // As this is the only thing in this file, for now we're not going to use this file anywhere.
  // Instead, the below function has just been inserted into header.php
  
  $(".menu-item-search").addClass("search-bar-tucked");

});
